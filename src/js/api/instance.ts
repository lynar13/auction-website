// src/js/api/instance.ts

/* Import the NoroffAPI class */

import NoroffAPI from '@api/index';  

/* Create and export an instance of NoroffAPI (no need to pass the base URL) */
export default new NoroffAPI();
